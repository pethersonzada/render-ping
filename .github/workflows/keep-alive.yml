name: Render Keep-Alive

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Acordar Servidor Render
        run: |
          curl -I https://rota-estudantil-backend.onrender.com/health
